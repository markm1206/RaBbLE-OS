---
# hardware/x64/asus_proart_p16/tasks/nvidia.yml — STUB
# Epoch I scaffolding only. Driver activation lives on mend-I/proart-nvidia.
- name: "asus_proart_p16/nvidia — stub"
  ansible.builtin.debug:
    msg: "spark ~ hardware/asus_proart_p16/nvidia >> stub, no-op // %DORMANT% (activation: mend-I/proart-nvidia)"
  tags: [hardware, nvidia]
