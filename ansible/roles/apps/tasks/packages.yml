---
# apps/tasks/packages.yml
- name: apps packages | install baseline desktop apps
  ansible.builtin.dnf:
    name:
      - dolphin
      - ark
      - ffmpegthumbs
      - kio-extras
    state: present
  tags: [apps, packages]
