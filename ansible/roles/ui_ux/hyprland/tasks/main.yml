---
# ui_ux/hyprland/tasks/main.yml — STUB
- { ansible.builtin.include_tasks: packages.yml, tags: [desktop, hyprland, packages] }
- { ansible.builtin.include_tasks: config.yml,   tags: [desktop, hyprland, config]   }
- { ansible.builtin.include_tasks: dotfiles.yml, tags: [desktop, hyprland, dotfiles] }
