---
# ui_ux/hyprland/tasks/dotfiles.yml — STUB
# This task file will symlink dotfiles/hyprland/ → ~/.config/hypr/
- name: hyprland dotfiles — stub
  ansible.builtin.debug:
    msg: "spark ~ ui_ux/hyprland >> dotfiles stub, no-op // %DORMANT%"
  tags: [desktop, hyprland, dotfiles]
