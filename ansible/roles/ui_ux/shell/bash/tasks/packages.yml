---
# ui_ux/shell/bash/tasks/packages.yml — STUB
- name: bash packages — stub
  ansible.builtin.debug:
    msg: "spark ~ ui_ux/shell/bash >> packages stub, no-op // %DORMANT%"
  tags: [desktop, shell, bash, packages]
