# hyprland-binds.conf.j2 — Launcher keybinds for Hyprland
# Managed by Ansible — desktop/launcher role

# App launcher
bind = SUPER, D, exec, fuzzel
# Emoji picker
bind = SUPER, Period, exec, rofimoji
