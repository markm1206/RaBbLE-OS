---
# ui_ux/terminal/tasks/packages.yml — STUB
- name: terminal packages — stub
  ansible.builtin.debug:
    msg: "spark ~ ui_ux/terminal >> packages stub, no-op // %DORMANT%"
  tags: [desktop, terminal, packages]
