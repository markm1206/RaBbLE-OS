---
# ui_ux/terminal/tasks/main.yml — STUB
- { ansible.builtin.include_tasks: packages.yml, tags: [desktop, terminal, packages] }
- { ansible.builtin.include_tasks: config.yml,   tags: [desktop, terminal, config]   }
